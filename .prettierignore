# Package Managers
package-lock.json
pnpm-lock.yaml
yarn.lock
src/lib/components/ui/

ai-dev-docs/

# Written by src/lib/server/chat/probe-payload.unit.test.ts, not by hand.
# Prettier would collapse the short arrays and the next test run would write
# them back — the file would flip on every commit.
scripts/ai/chat-tools.json

# Inlang (managed by paraglide)
project.inlang/

# Local design-hook cache, not source
.impeccable/

# Agent scratch worktrees (11 GB of checkouts, not source)
.claude/worktrees/

# Agent scratch dir (3 GB of checkout copies, not source)
.scratch/
