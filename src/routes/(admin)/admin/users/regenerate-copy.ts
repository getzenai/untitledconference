/**
 * What "Regenerate link" means: the previous token dies.
 *
 * One sentence, used by the tooltip, the success dialog, and the test, so
 * the button cannot drift back into "send another copy of the same invite".
 */
export const REGENERATE_REPLACES_LINK_COPY = 'The previous invitation link no longer works.';
