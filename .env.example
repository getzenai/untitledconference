# All secrets are fetched from Azure Key Vault. Run:
#   ./scripts/azure-managed-setup/dev-from-kv.sh
# See scripts/azure-managed-setup/CLAUDE.md for setup instructions.

# Non-secret configuration (optional overrides — dev-from-kv.sh sets sensible defaults)
# LOG_LEVEL="debug"
# LOG_FORMAT="human"

# Test database (local Docker — not managed by Azure)
TEST_DATABASE_URL="postgres://root:mysecretpassword@localhost:5433/test"
